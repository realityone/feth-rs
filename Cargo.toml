[package]
name = "feth-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2"
nix = { version = "0.29", features = ["ioctl"] }

[dev-dependencies]
clap = { version = "4", features = ["derive"] }
